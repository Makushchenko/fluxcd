apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo
  namespace: kbot
  flux: test1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-kbot
  template:
    metadata:
      labels:
        app: demo-kbot
    spec:
      imagePullSecrets:
        - name: ghcr-creds
      containers:
        - name: kbot     
          image: ghcr.io/makushchenko/kbot:v1.0.4-6e0844b-linux-amd64
          imagePullPolicy: Always
          command: ["/kbot"]
          args: ["start"]         
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
          volumeMounts:
            - mountPath: /dev/mem
              name: dev-mem
          env:
            - name: TELE_TOKEN
              valueFrom:
                secretKeyRef:
                  name: kbot
                  key: token
      volumes:
        - hostPath:
            path: /dev/mem
            type: ""
          name: dev-mem